# src/app/api — Server API routes

Purpose: Server-side handlers for application API endpoints.

Location & pattern:

Adding a new endpoint:

1. Create `src/app/api/myRoute.api.ts`.
2. Validate the request body using schemas from `src/types/`.
3. Return JSON responses with appropriate status codes.

Security & validation:

Testing:
